
#1.1.8

#[gtk-about]

[Icon]

#F[Icon]
frame_maininfo=

#d Name of the dock it belongs to:
dock name=_MainDock_

#s[Default] Name of the icon as it will appear in its caption in the dock:
name=
#v
sep_display=

#S+[Default] Image filename:
icon=

#j+[0;128] Desired icon size for this applet
#{Set to 0 to use the default applet size}
icon size=0;0;

order=3.75

#A
handbook=Clipper


#[gtk-convert]

[Desklet]

#X[Position]
frame_pos=

#b Lock position?
#{If locked, the desklet cannot be moved by simply dragging it with the left mouse button. It can still be moved with ALT + left-click.}
locked=false

#j[24;512] Desklet dimensions (width x height):
#{Depending on your WindowManager, you may be able to resize this with ALT + middle-click or ALT + left-click.}
size=96;96;

#i[-2048;2048] Desklet position (x, y):
#{Depending on your WindowManager, you may be able to move this with ALT + left-click.}
x position=-655
#i[-2048;2048] ...
y position=-391

#I[-180;180] Rotation:
#{You can quickly rotate the desklet with the mouse, by dragging the little buttons on its left and top sides.}
rotation=0

#X[Visibility]
frame_visi=

#b Is detached from the dock
initially detached=false
#l[Normal;Keep above;Keep below;Keep on widget layer;Reserve space] Visibility:
accessibility=0
#b Should be visible on all desktops?
sticky=true

#F[Decorations;gtk-orientation-portrait]
frame_deco=

#o+ Choose a decoration theme for this desklet:
#{Choose 'Custom decorations' to define your own decorations below.}
decorations=default

#v
sep_deco=

#S+ Background image:
#{Image to be displayed below drawings, e.g. a frame. Leave empty for no image.}
bg desklet=
#e+[0;1] Background transparency:
bg alpha=1
#i+[0;256] Left offset:
#{in pixels. Use this to adjust the left position of drawings.}
left offset=0
#i+[0;256] Top offset:
#{in pixels. Use this to adjust the top position of drawings.}
top offset=0
#i+[0;256] Right offset:
#{in pixels. Use this to adjust the right position of drawings.}
right offset=0
#i+[0;256] Bottom offset:
#{in pixels. Use this to adjust the bottom position of drawings.}
bottom offset=0
#S+ Foreground image:
#{Image to be displayed above the drawings, e.g. a reflection. Leave empty for no image.}
fg desklet=
#e+[0;1] Foreground tansparency:
fg alpha=1

use size=
no input=0
depth rotation y=0
depth rotation x=0

width=92
height=92

num desktop=-1

[Configuration]

#F[Items;gtk-convert]
frame_item=

#l[None;Clipboard;Selection;Both] Which items should be remembered?
#{Clipboard items are those you get with CTRL+c. Selection items are those you get by selecting some text with the mouse.}
item type=1

#I[1;50] Number of items:
nb items=50

#b Remember items between 2 sessions ?
remember=true

last items=下一站,说爱你=-+-=克丽丝·霍旺=-+-=泰版浪漫满屋=-+-=			/* Libnet Context for First ARP Packet */=-+-=			/* Libnet Context for Second ARP Packet */=-+-=https://github.com/lewang/rebox2=-+-=http://www.youtube.com/watch?v=53YeTdVtDkU=-+-=Try [rebox2](https://github.com/lewang/rebox2), it is highly configurable and powerful, there is a [demo](http://www.youtube.com/watch?v=53YeTdVtDkU) uploaded by the author.=-+-=(add-hook 'emacs-lisp-mode-hook\n		  (lambda ()\n			(set (make-local-variable 'rebox-style-loop) '(21 25 111))\n			(rebox-mode 1)\n			))\n(add-hook 'c-mode-hook\n		  (lambda ()\n			(set (make-local-variable 'rebox-style-loop) '(243 241 111))\n			(rebox-mode 1)\n			))=-+-=    (add-hook 'emacs-lisp-mode-hook\n    		  (lambda ()\n    			(set (make-local-variable 'rebox-style-loop) '(21 25 111))\n    			(rebox-mode 1)\n    			))\n    (add-hook 'c-mode-hook\n    		  (lambda ()\n    			(set (make-local-variable 'rebox-style-loop) '(243 241 111))\n    			(rebox-mode 1)\n    			))=-+-=	;=-+-=https://github.com/remyferre/comment-dwim-2=-+-=tuhdo=-+-=(global-set-key (kbd "M-;") 'comment-dwim-2)=-+-=(setq comment-dwim-2--inline-comment-behavior 'reindent-comment)=-+-=repeating comment-dwim-2 will by default reindent the comment instead of killing it, and that calling comment-dwim-2 with a prefix argument will kill the comment instead of reindenting it.=-+-=comment-dwim-2=-+-=(setq comment-style 'extra-line)\n=-+-=use rebox2 instead of builtin comment-box\n\n=-+-=comment-dwim can also be repeated several times to switch between the different behaviors.=-+-= ;sadd =-+-=C-M-; to 'comment-or-uncomment-region-or-line\n=-+-= ;=-+-=http://www.douyutv.com/2246 =-+-=对不起青春=-+-=世界奇妙物语2014=-+-=惊喜=-+-=http://ftp.gnu.org/gnu/emacs/emacs-24.4.tar.xz=-+-=ffer is for notes you don't \nwant to create a file, visit\nter the text in that file's =-+-=下弦之月=-+-=HYDE=-+-= save, and \nile with C-\nfer.       =-+-=--no-splash=-+-=splash=-+-=highlighting\nselect=-+-=prune=-+-=http://oli.me.uk/2014/10/20/making-package-el-behave-like-vundle/=-+-=https://github.com/Wolfy87/dotfiles/blob/d24591ebd7b3a36f629fb5a4ebd921c72f2b5b91/emacs/init.el#L61-L96=-+-=http://www.reddit.com/r/emacs/comments/2jtojf/packageel_didnt_prune_my_unused_packages_so_i/=-+-=simpler to get it working and to configure it=-+-=a lot of backends=-+-= gaining more ground=-+-=File=-+-=Edit=-+-=File Edit Options Buffers Tools Lisp-Interaction Help                                                                                   =-+-=Options=-+-=out=-+-=习=-+-=--fedora=-+-=http://lttng.org/docs/

#B Separate clipboard and selection?
#{It is especially useful if you often select text with the mouse and don't want to loose your clipboard items due to too many items in the selection.}
separate=false

#I[1;50] If so, number of selection items:
nb items2=10

#b Paste into Clipboard?
#{When you click on an item, its content will become accessible with CTRL+v}
paste clipboard=true

#b Paste into Selection?
#{When you click on an item, its content will become accessible with the middle-click}
paste selection=false

#k Shortkey to pop-up the items menu:
shortkey=<Control>F8

#b Pop-up menus at mouse position?
menu on mouse=true


#F[Actions;gtk-execute]
frame_act=

#B[2] Enable actions?
#{If some actions are associated with an item, they will be proposed when the item is created.}
enable actions=false

#b Replay actions?
#{Display actions when selecting an item in the history.}
replay action=true

#I[1;12] Duration of the action menu:
#{in seconds.}
action duration=4


#F[Persistent items;gtk-dnd-multiple]
frame_per=

#U A list of persistent items which can be accessed with middle-click:/
persistent=
