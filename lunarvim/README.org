# Before using this configs, backup ~/.vim, ~/.cache/nvim, ~/.config/nvim, ~/.local/share/nvim
# $ stowsh -r spacevim
# install:
# in bash:
$ bash <(curl -s https://raw.githubusercontent.com/lunarvim/lunarvim/master/utils/installer/install.sh)
# in fish
$ bash -c "bash <(curl -s https://raw.githubusercontent.com/lunarvim/lunarvim/master/utils/installer/install.sh)"
# uninstall:
$ ~/.local/share/lunarvim/lvim/utils/installer/uninstall.sh
# If lots of errors after reinstall, exucute:
# :LvimCacheReset and :PackerSync
