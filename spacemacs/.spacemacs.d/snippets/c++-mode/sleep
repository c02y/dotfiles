# -*- mode: snippet -*-
# name: std::this_thread::sleep_for
# key: sleep
# --
#include <thread>
std::this_thread::sleep_for(std::chrono::${1:$$(yas-choose-value '("seconds" "milliseconds" "microseconds" "nanoseconds" "minutes" "hours" "..."))}($0));