# -*- mode: snippet -*-
# name: impl From<From> for Type { fn from(...) }
# key: impl
# --
impl${1:<'a>} From<${2:From}> for ${3:Type}$1 {
    fn from(source: $2) -> Self {
       $0
       Self { }
    }
}
